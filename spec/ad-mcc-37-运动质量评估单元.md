# ad-mcc-37-运动质量评估单元 接口规格

---

## 基本信息

| 项 | 内容 |
|----|------|
| 模块编号 | ad-mcc-37 |
| 模块名称 | 运动质量评估单元 |
| 所属分区 | 十、执行反馈与日志 |
| 核心职责 | 基于 ad-mcc-36 提供的各执行模块（转向、动力、制动、姿态）偏差数据汇总包，以及各模块在执行过程中上报的冲击度数据，量化评估每次操控动作的运动质量。计算纵向冲击度RMS、横向冲击度RMS、偏差累积量等关键指标，综合输出运动质量得分（0–100分）与优化建议。为 ECC 元认知模块、运动调优及驾驶员体验评估提供量化依据。不参与任何操控决策，仅负责运动质量的评估与报告 |
| 依赖模块 | ad-mcc-36（执行闭环反馈单元，提供偏差数据汇总包）、ad-mcc-10（纵向冲击度约束单元，可选提供纵向冲击度数据）、ad-mcc-06（横向冲击度约束单元，可选提供横向冲击度数据） |
| 被依赖模块 | ad-mcc-01（小脑总控调度核心，接收运动质量评估报告）、ad-mcc-03（全身运动状态归集中心，接收质量得分用于状态汇总）、ECC-08（元认知模块，通过 CerebellumBus 接收质量报告供自省与调优）、ad-mcc-38（执行日志记录单元，记录质量评估结果） |


## 内部状态定义

| 状态 | 标识 | 含义 | 触发条件 |
|------|------|------|----------|
| 正常评估 | `NORMAL_EVALUATION` | 正常进行运动质量评估 | 系统初始化完成，收到偏差数据包 |
| 数据不足 | `INSUFFICIENT_DATA` | 收到的偏差数据不足以完成评估，等待更多数据 | 偏差数据包中关键维度缺失 |
| 暂停服务 | `SYSTEM_PAUSED` | 系统紧急熔断 | 收到紧急熔断指令 |


## 输入数据

| 输入项 | 数据类型 | 来源模块 | 触发条件 | 优先级 |
|--------|----------|----------|----------|:---:|
| 偏差数据汇总包 | Struct（指令ID + 各维度偏差明细：转向角度偏差、速率偏差、动力速度偏差、制动压力偏差、姿态偏差 + 各模块响应延迟） | ad-mcc-36 执行闭环反馈单元 | 每次闭环回执生成后 | **高** |
| 纵向冲击度序列（可选） | List（冲击度_m_s3 + 时间戳） | ad-mcc-10 纵向冲击度约束单元 | 每次动力指令执行周期内 | 普通 |
| 横向冲击度序列（可选） | List（冲击度_m_s3 + 时间戳） | ad-mcc-06 横向冲击度约束单元 | 每次转向指令执行周期内 | 普通 |
| 全局调度指令 | Enum（暂停/恢复/熔断） | ad-mcc-01 小脑总控调度核心 | 模式切换或紧急事件时 | **紧急** |


## 输出数据

| 输出项 | 数据类型 | 目标模块 | 输出条件 | 优先级 |
|--------|----------|----------|----------|:---:|
| 运动质量评估报告 | Struct（指令ID + 运动质量得分 + 纵向冲击度RMS + 横向冲击度RMS + 偏差累积量 + 综合评级 + 优化建议列表） | ad-mcc-01（内部调度总线）、ad-mcc-03（内部调度总线）、ECC-08（通过 CerebellumBus） | 每次评估完成后 | **高** |
| 质量趋势记录 | Struct（指令ID + 质量得分 + 关键指标） | ad-mcc-38 执行日志记录单元 | 每次评估完成后 | 普通 |


## 评估模型与指标

### 一、评估指标

| 指标 | 计算方式 | 单位 | 方向 |
|------|----------|------|------|
| 纵向冲击度 RMS | sqrt(mean(纵向冲击度²)) | m/s³ | 越小越好 |
| 横向冲击度 RMS | sqrt(mean(横向冲击度²)) | m/s³ | 越小越好 |
| 转向偏差累积 | Σ\|角度偏差\| × dt | °·s | 越小越好 |
| 速度偏差累积 | Σ\|速度偏差\| × dt | km/h·s | 越小越好 |
| 制动压力偏差累积 | Σ\|压力偏差\| × dt | MPa·s | 越小越好 |
| 响应延迟最大值 | max(各模块响应延迟) | ms | 越小越好 |

### 二、综合得分映射（0–100分）

| 得分区间 | 评级 | 含义 |
|:---:|:---:|------|
| 90–100 | 优秀 | 运动控制平顺，偏差极小，响应迅速 |
| 75–89 | 良好 | 偶有轻微偏差或冲击度偏高，整体舒适 |
| 60–74 | 一般 | 存在可感知的顿挫或偏差，需优化 |
| 40–59 | 较差 | 频繁明显顿挫或较大偏差，影响乘坐体验 |
| 0–39 | 很差 | 严重冲击或偏差，可能涉及安全或机械问题 |

综合得分 = 纵向冲击度得分 × 0.25 + 横向冲击度得分 × 0.25 + 偏差控制得分 × 0.3 + 响应延迟得分 × 0.2

其中各维度得分根据实际值与理想值的偏差进行线性或分段映射。

### 三、优化建议生成规则

| 触发条件 | 建议内容 |
|----------|----------|
| 纵向冲击度 RMS > 3.0 m/s³ | 建议降低加速/减速速率，检查油门与制动平顺控制参数 |
| 横向冲击度 RMS > 2.0 m/s³ | 建议降低转向速率或检查横向冲击度约束阈值 |
| 转向偏差累积超标 | 建议检查转向执行器响应或转向系统标定 |
| 制动压力偏差累积超标 | 建议检查制动主缸压力传感器或制动系统气阻 |
| 响应延迟最大值 > 200ms | 建议检查通信总线负载或控制器算力 |


## 核心处理逻辑

```
FUNCTION motion_quality_evaluation_main_loop():
    STATE_NORMAL = NORMAL_EVALUATION
    STATE_INSUFF = INSUFFICIENT_DATA
    STATE_PAUSED = SYSTEM_PAUSED

    SET internal_state = STATE_NORMAL
    初始化指标历史缓冲区（可选，用于趋势分析）

    WHILE 系统运行中:
        // 第1步：紧急熔断
        IF 收到紧急熔断指令:
            SET internal_state = STATE_PAUSED
            CONTINUE
        ELSE IF 收到恢复指令 AND internal_state == STATE_PAUSED:
            SET internal_state = STATE_NORMAL

        // 第2步：接收偏差数据汇总包
        IF 收到 ad-mcc-36 偏差数据汇总包:
            指令ID = 数据包.指令ID
            偏差明细 = 数据包.偏差明细

            // 校验数据完整性
            IF 偏差明细中缺少关键维度（转向/动力/制动至少其一）:
                SET internal_state = STATE_INSUFF
                CONTINUE

            // 第3步：获取冲击度序列（如有）
            纵向冲击度序列 = 获取纵向冲击度序列(指令ID对应时间段)
            横向冲击度序列 = 获取横向冲击度序列(指令ID对应时间段)

            // 第4步：计算各指标
            纵向冲击度RMS = 计算RMS(纵向冲击度序列) IF 序列非空 ELSE 0.0
            横向冲击度RMS = 计算RMS(横向冲击度序列) IF 序列非空 ELSE 0.0

            转向偏差累积 = 计算累积偏差(偏差明细.转向.角度偏差序列)
            速度偏差累积 = 计算累积偏差(偏差明细.动力.速度偏差序列)
            制动偏差累积 = 计算累积偏差(偏差明细.制动.压力偏差序列)

            响应延迟最大值 = 提取各模块响应延迟最大值(偏差明细)

            // 第5步：各维度得分
            纵向冲击度得分 = 映射得分(纵向冲击度RMS, 理想=0, 差=5.0, 反向)
            横向冲击度得分 = 映射得分(横向冲击度RMS, 理想=0, 差=3.0, 反向)
            偏差控制得分 = 映射综合偏差得分(转向偏差累积, 速度偏差累积, 制动偏差累积)
            响应延迟得分 = 映射得分(响应延迟最大值, 理想=0, 差=300ms, 反向)

            // 第6步：综合得分
            综合得分 = 纵向冲击度得分 × 0.25 + 横向冲击度得分 × 0.25 + 偏差控制得分 × 0.3 + 响应延迟得分 × 0.2

            // 第7步：综合评级
            综合评级 = 根据综合得分映射评级(综合得分)

            // 第8步：生成优化建议
            优化建议列表 = 根据各指标阈值生成优化建议(
                纵向冲击度RMS, 横向冲击度RMS,
                转向偏差累积, 速度偏差累积, 制动偏差累积,
                响应延迟最大值
            )

            // 第9步：构建并输出评估报告
            评估报告 = 构建运动质量评估报告(
                指令ID, 综合得分,
                纵向冲击度RMS, 横向冲击度RMS,
                转向偏差累积, 速度偏差累积, 制动偏差累积,
                综合评级, 优化建议列表
            )

            向 ad-mcc-01 发送运动质量评估报告
            向 ad-mcc-03 发送运动质量评估报告
            向 ECC-08 发送运动质量评估报告（通过 CerebellumBus）
            向 ad-mcc-38 发送质量趋势记录(指令ID, 综合得分, 关键指标)

            SET internal_state = STATE_NORMAL

        SLEEP 50ms
```


## 约束与异常处理

| 场景 | 处理方式 | 恢复条件 |
|------|----------|----------|
| 偏差数据包中关键维度缺失 | 标记为数据不足，暂不评估，记录日志 | 收到完整数据包 |
| 冲击度序列数据不可用 | 对应指标得分使用默认中性值（50分），在报告中标注 | 冲击度数据恢复可用 |
| 计算过程中出现异常值（如RMS过大） | 截断至合理上限，记录异常标记 | — |
| 评估结果得分连续低于40分 | 向上层发出持续性运动质量告警 | 质量恢复 |
| 紧急熔断 | 暂停评估，保持最后评估结果 | 紧急解除后恢复 |


## 总线契约

| 总线 | 操作 | 数据内容 | 权限 | 说明 |
|------|------|----------|------|------|
| 内部调度总线 | 读 | 偏差数据汇总包 | 只读 | ad-mcc-36 发送 |
| 内部调度总线 | 读 | 纵向冲击度序列（可选） | 只读 | ad-mcc-10 提供 |
| 内部调度总线 | 读 | 横向冲击度序列（可选） | 只读 | ad-mcc-06 提供 |
| 内部调度总线 | 写 | 运动质量评估报告 | 专属写入 | 向 ad-mcc-01、ad-mcc-03 发送 |
| CerebellumBus | 写 | 运动质量评估报告 | 专属写入 | 向 ECC-08 发送 |
| 内部调度总线 | 写 | 质量趋势记录 | 事件触发写入 | 向 ad-mcc-38 发送 |


## 安全边界

| 规则编号 | 内容 |
|:---:|------|
| S-01 | 运动质量评估结果仅供上层模块参考与优化，不直接干预底层执行器控制 |
| S-02 | 数据不足时不得强行评估，必须明确标记评估结果的可信度 |
| S-03 | 评估指标计算必须基于实际上报数据，不得美化或篡改 |
| S-04 | 本模块仅负责运动质量的量化评估，不参与任何操控决策 |


## 接口校验用例

| 用例编号 | 前置条件 | 输入 | 预期输出 |
|----------|----------|------|----------|
| TC-M37-01 | 收到完整偏差数据包，冲击度可用 | 偏差汇总包 + 低冲击度序列 | 综合得分 ≥ 85，评级优秀，优化建议为空 |
| TC-M37-02 | 收到偏差数据包，冲击度偏高 | 纵向冲击度 RMS=4.0，横向 RMS=2.5 | 综合得分 < 70，评级一般，含冲击度优化建议 |
| TC-M37-03 | 偏差数据包缺少转向维度 | 偏差明细中无转向数据 | 标记数据不足，不输出评估报告 |
| TC-M37-04 | 冲击度序列不可用 | 冲击度序列为 None | 对应维度得分=50，报告中标注“冲击度数据不可用” |
| TC-M37-05 | 综合得分连续低于 40 分 | 连续3次评估得分 < 40 | 发出持续性运动质量告警 |
| TC-M37-06 | `SYSTEM_PAUSED`，收到熔断 | 紧急熔断指令 | 暂停评估，保持最后评估结果 |


## 质量自检清单

| 检查项 | 状态 |
|--------|:---:|
| 模块编号与分区归属正确 | ✅ |
| 依赖与被依赖模块编号完整 | ✅ |
| 内部状态机3个状态含触发条件 | ✅ |
| 输入/输出含数据类型、来源/目标模块、优先级 | ✅ |
| 评估模型完整（6项指标 + 综合得分映射 + 优化建议规则） | ✅ |
| 核心处理逻辑伪代码覆盖数据校验、指标计算、得分映射、建议生成全流程 | ✅ |
| 约束与异常覆盖数据不足、冲击度缺失、异常值、持续低分、熔断共5条 | ✅ |
| 总线契约区分内部调度总线与CerebellumBus | ✅ |
| 安全边界逐条列出 | ✅ |
| 校验用例覆盖高分、低分、数据缺失、冲击度缺失、持续告警、熔断共6条 | ✅ |